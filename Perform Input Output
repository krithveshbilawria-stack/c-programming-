#include <stdio.h>

int main() {
    char ch;
    int num;
    float f;
    double d;

    // Read values in the given order
    scanf("%c %d %f %lf", &ch, &num, &f, &d);

    // Print values with formatting
    printf("%c %d %.2f %.6lf\n", ch, num, f, d);

    return 0;
}

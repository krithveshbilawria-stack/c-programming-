#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float p,r,t,CI;
    scanf("%f%f%f%f",&p,&r,&t,&CI);
    CI=p*(pow((1+r/100),t)-1);
    printf("%.2f",CI);
    return 0;
}

